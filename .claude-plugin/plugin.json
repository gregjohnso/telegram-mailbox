{
  "name": "telegram-mailbox",
  "description": "Telegram mailbox for Claude Code — inbound messages persist to mailbox.jsonl and are drained by /telegram-mailbox:mailbox-check inside a /loop. No --channels flag required. Forked from anthropics/claude-plugins-official telegram plugin.",
  "version": "0.0.1",
  "keywords": [
    "telegram",
    "messaging",
    "mailbox",
    "mcp"
  ]
}
